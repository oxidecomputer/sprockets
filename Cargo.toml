[workspace]
members = [
    "hyper-sprockets",
    "rot",
    "host",
    "common",
    "session",
    "proxy",
]

default-members = [
    "hyper-sprockets",
    "rot",
    "host",
    "common",
    "session",
    "proxy",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

